import Nav from "./Nav";
import classes from "./HeaderMenu.module.css";
const DeskNav = () => {
  return (
    <nav className={classes.DeskNav}>
      <Nav />
    </nav>
  );
};

export default DeskNav;
